// With this we can increase Maintability
// We can reuse
// We can apply abstraction principle

// In ES5 we didnt had modules (we used module formats)

// I will focus on CommonJS(Node.js) module and ES6(browsers)