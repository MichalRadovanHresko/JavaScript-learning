const first = [1,4,10];
const second = [10,2];

const combined = [...first, ...second];
// With this we can even add element between or at the end 
// const combined = [ ...first, 'a',...second, 'b'];

const copy = [...copy];
console.log(combined);
console.log(copy);