let number = Math.floor(Math.random()*1000);

if (number>0) {
    console.log('The number is positive! and the number is ' + number);
} else if(number==0) {
    console.log('The number is neutral! and the number is ' + number);
} else {
    console.log('The number is negative! and the number is ' + number);
}